enum MaterialType {
  container = 'container',
  text = 'text',
  link = 'link',
  image = 'image',
  input = 'input',
  radio = 'radio',
  checkbox = 'checkbox',
  form = 'form',
  list = 'list',
  table = 'table',
  tree = 'tree',
  thirdParty = 'thirdParty',
  custom = 'custom',
  component = 'component',
}

export default MaterialType;
