enum EventType {
  click = 'click',
  touch = 'touch',
  mouseenter = 'mouseenter',
  mouseleave = 'mouseleave',
  scroll = 'scroll',
  exposure = 'exposure',
  interval = 'interval',
  refreshData = 'refreshData',
}

export default EventType;
